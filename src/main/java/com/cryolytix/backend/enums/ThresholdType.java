package com.cryolytix.backend.enums;

public enum ThresholdType {
    SENSOR,  // Thresholds applied to sensors (e.g., Temperature, Humidity)
    DEVICE   // Thresholds applied to device-level values (e.g., Speed, Altitude)
}
